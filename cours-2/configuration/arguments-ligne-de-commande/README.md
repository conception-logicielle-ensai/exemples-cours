# Pour lancer le programme

Pour lancer le programme, il suffit d’exécuter la commande suivante depuis le répertoire courant :

```
python3 ./cli_variables.py app.log
```

Le programme créera un fichier de journalisation nommé `app.log`.

Vous pouvez remplacer `app.log` par le nom de fichier de votre choix.