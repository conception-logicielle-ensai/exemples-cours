# Exemple Cython simple

Ce projet montre comment utiliser Cython pour accélérer du code Python.

## Prérequis

- Python 3.8+
- pip
- C compiler (gcc, clang, MSVC)

Installer Cython :
```
pip install -r requirements.txt
```

Compilation:

```
python setup.py build_ext --inplace
```